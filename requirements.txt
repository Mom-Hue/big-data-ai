# 课程《大数据与人工智能》所需的 Python 第三方库
# 安装方式（在终端执行一行即可）：
#   pip install -r requirements.txt
# 覆盖：表格数据、中文分词、情感分析、文本分类、可视化

pandas>=2.0          # 表格数据读写与统计
numpy>=1.24          # 数值计算（pandas / scikit-learn 依赖）
matplotlib>=3.7      # 图表可视化
jieba>=0.42          # 中文分词
snownlp>=0.12        # 中文情感分析
scikit-learn>=1.3    # 机器学习（文本分类等）
